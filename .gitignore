node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
runtime-settings.json

# Uploaded videos & metadata in uploads/ ARE saved to git (persist across projects).
# Only large fingerprint result files are excluded (GitHub blocks files > 100MB).
uploads/*_result.json

# Stray test videos in the repo root stay ignored
/*.mp4
/*.mov
/*.avi
/*.webm
/*.mkv
